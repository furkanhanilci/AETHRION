# Component Adoption Register

| Field | Value |
|---|---|
| Document type | Index — **generated** from `provenance/components.json` |
| Scope | Every external component this system runs on, what AETHRION still owns, and what the component may never decide |
| Sibling documents | `upstreams.json` / [`README.md`](README.md) (mechanisms assimilated into this repository's own code) · `../docs/architecture/AETHRION_COMPONENT_REUSE.md` (why each is adopted) |
| Status | Regenerated by `scripts/check_wp_implementation_sources.py --write`; **never edited by hand** |
| Entries | **51** |
| Bound to | **48** work packages |
| Open obligations | **71** |
| Register retrieved | 2026-08-24 |

**In one paragraph.** This register answers *which running implementation does this control stand on*, and its sibling answers *which mechanisms are implemented here having been solved somewhere else first*. The distinction is not pedantic: a `DEPENDENCY` is installed and called and the work happens inside it, while an assimilated mechanism runs as this system's own code and leaves no runtime trace of where it came from. The two create different obligations, which is why they are two registers. Every entry below was already decided in `AETHRION_COMPONENT_REUSE.md`, an ADR or the package that owns the integration; what is new is that the decision is now machine-readable and joined to the package that has to execute it.

> **A null obligation field is not an oversight.** It is an unresolved acquisition decision, it is printed in the package that carries it, and `scripts/ready_queue.py` will not call that package `READY` until it is answered. Every entry is `PROPOSED` except the two that actually run.

---

## Adoption types

| Type | Meaning | Obligation before implementation |
|---|---|---|
| **DEPENDENCY** | Called at runtime, not copied. Version pinning, failure semantics and an upgrade path are the obligation. | version or image-digest policy · failure semantics |
| **ADAPTER** | An external component held behind an AETHRION contract. The obligation is that the contract survives replacing it. | version or image-digest policy · failure semantics |
| **OPTIONAL_BACKEND** | A deployment choice behind a capability interface. The interface is the contract, not the backend, and the choice must be recorded rather than made implicitly. | a recorded qualification, and the backend actually chosen |
| **STANDARD** | A format or model implemented as specified. Conformance testing is the obligation. | a conformance suite against the published specification |
| **PATTERN** | An architectural idea implemented here with no code taken and nothing called. Attribution and honesty about divergence are the obligation. | attribution, and honesty about divergence |

---

## DEPENDENCY (25)

| ID | Component | Packages | Status | AETHRION owns | May never decide |
|---|---|---|---|---|---|
| `CMP-001` | Temporal | `WP-031` · `WP-032` | `PROPOSED` | The G0–G10 gate lifecycle and its transition authority. Workflow definitions, the ProjectLifecycle skeleton and every gate transition are AETHRION's. | Temporal is the process authority — it decides when a transition may be attempted, never whether the evidence supports it. A workflow may not compute a gate verdict; it calls the Gate Service and records what came back. |
| `CMP-002` | NATS JetStream | `WP-028` | `PROPOSED` | The event envelope, subject taxonomy and the transactional outbox that decides what is published at all. | The event plane carries events and never authority. An event is a notification that something was decided elsewhere; nothing becomes true because a message arrived, and no consumer may treat an event as a gate input. |
| `CMP-003` | LangGraph | `WP-046` | `PROPOSED` | The bounded-cognition runtime contract: what a task may reason over, for how long, and with which context projection. | Cognition is bounded inside one task and never spans a gate. A LangGraph run may produce a hypothesis, a draft or a recommendation; it may not carry state across a gate boundary and it holds no verdict authority. |
| `CMP-005` | Inspect AI | `WP-043` · `WP-048` | `PROPOSED` | The behaviours themselves, their pass criteria, the golden sets and the contamination controls — encoded as Inspect tasks, solvers and scorers. | A scorer result is a `VerificationResult`, never a `GateRecord` verdict. Inspect measures; gate policy decides. An Inspect transcript is operational evidence and is not the canonical run record. |
| `CMP-006` | GROBID | `WP-061` · `WP-063` | `PROPOSED` | `SourceRepresentation` — which parser, which version, which digest produced the representation a span is anchored to. | A parser produces a representation, never an interpretation. A re-parse creates `representation-v2` and does not invalidate a claim anchored to v1; the claim stays bound to the representation that actually supported it. |
| `CMP-007` | Pub2TEI | `WP-061` | `PROPOSED` | The requirement that a span means the same thing regardless of where the source came from. | Normalisation may change representation and never meaning. A publisher-supplied structure that disagrees with the PDF is a recorded discrepancy, not a silent preference. |
| `CMP-017` | MLflow | `WP-029` · `WP-082` | `PROPOSED` | The Run Registry, the preflight validator and the binding from a run to a `RawEvaluatorArtifact` and a `VerifiedValue`. | MLflow answers *what did the system do*. It never answers *what may be believed* — that is a signed `EvidenceManifest` plus Workflow Run RO-Crate. Operational telemetry is not provenance, and an MLflow metric is not a `VerifiedValue`. |
| `CMP-018` | OpenTelemetry | `WP-096` | `PROPOSED` | The end-to-end correlation identifier standard — WP-011 — and which spans must exist for a run to be reconstructable. | Telemetry is operational and never evidential. A missing span is an observability defect; it is not proof that a step did not happen, and a present span is not proof that it did. |
| `CMP-019` | sigstore-python | `WP-059` · `WP-027` | `PROPOSED` | The attestation profile and what a signature is taken to mean. The named upgrade out of `airl-interim-v0.1`. | A signature establishes who issued an artifact and that it has not changed. It never establishes that the content is correct, and a verified signature is not an acceptance. |
| `CMP-020` | OpenSSF model-signing | `WP-044` | `PROPOSED` | The requirement that a local open-weight model file has a verified identity before it is admitted. | A signed model file is an admitted *artifact*, not a qualified *actor*. Qualification is WP-044's measurement. |
| `CMP-021` | OpenTimestamps | `WP-139` | `PROPOSED` | What is anchored, and the statement that today's profile is tamper-evident but not externally witnessed. | An anchor establishes that something existed by a time. It says nothing about who produced it or whether it is true. |
| `CMP-026` | LinkML | `WP-020` | `PROPOSED` | The single contract model from which JSON Schema, Pydantic, JSON-LD, SHACL and SQL DDL are generated. | A generator produces representations of a model; it never decides the model. A generated artifact that disagrees with the model is a defect in the model or the generator, never a third opinion. |
| `CMP-027` | Pandoc | `WP-090` | `PROPOSED` | Every AIRL transformation, expressed as an AST filter. | A renderer formats what the compiler decided; it never changes what a document asserts. **AIRL transformations are AST filters, never regexes over a manuscript.** |
| `CMP-028` | Quarto | `WP-090` | `PROPOSED` | The publication package contract and what a rendered artifact must contain. | As for Pandoc: rendering never changes an assertion. |
| `CMP-031` | veraPDF | `WP-090` | `PROPOSED` | The accessibility contract the rendered artifact must satisfy. | A validator reports conformance to a format profile. It does not assess whether the document is readable by a person, and a pass is not an accessibility claim. |
| `CMP-032` | statcheck · grim · pysprite | `WP-087` | `PROPOSED` | Which forensic test runs at which gate, and what a failure means for the claim. | A forensic finding is a `VerificationResult` that opens an investigation. It is never a finding of misconduct and never a gate verdict — `investigating-integrity-concerns` governs what happens next. |
| `CMP-033` | krippendorff · statsmodels · scikit-learn | `WP-126` · `WP-089` | `PROPOSED` | Which estimator answers which question, and the decision rule written before the data is seen. | An agreement coefficient measures agreement, not correctness. Two reviewers who agree may both be wrong, and a high coefficient is never evidence that a verdict is right. |
| `CMP-036` | PostgreSQL | `WP-025` | `PROPOSED` | The canonical record model and which registry is the authority for which field — WP-012's field-level authority matrix. | The database stores canonical records; it never decides what is canonical. Two stores holding the same field is a defect in the ownership matrix, not a reconciliation problem to solve at query time. |
| `CMP-037` | Neo4j · pgvector · OpenSearch | `WP-030` | `PROPOSED` | The projection contracts and the rebuild that proves a derived model can be destroyed and reconstructed from the canonical store. | A derived read model is never a source of truth. Anything that exists only in a projection is lost by definition, and a projection that disagrees with the canonical store is rebuilt rather than corrected. |
| `CMP-038` | Kubernetes | `WP-052` | `PROPOSED` | The trust-zone segmentation and the `ExecutionProfile` that decides where a workload may run. | The scheduler places work that policy already permitted. Placement is never a permission, and a namespace boundary is not the trust boundary — ADR-003 puts that at the capability. |
| `CMP-039` | Kueue | `WP-053` | `PROPOSED` | The quota and priority policy, and the rule that budget pressure degrades verbosity rather than assurance. | A queue decides when work runs, never whether it may. A task that cannot afford its required assurance is `BLOCKED`, not run more cheaply — ADR-011. |
| `CMP-040` | SPIFFE / SPIRE and Vault | `WP-055` | `PROPOSED` | Which identity may hold which capability, and the scoped, short-lived credential injected only after a `PolicyDecision`. | An identity system proves who is asking. It never decides what the asker may do — that is the policy decision point, and a valid identity with no grant is a denial. |
| `CMP-041` | Langfuse | `WP-097` | `PROPOSED` | Prompt governance: which prompt version ran, under which policy, against which model fingerprint. | A trace records what a model was asked and what it said. It is operational evidence and never a scientific record; a prompt that produced a good answer is not thereby a qualified prompt. |
| `CMP-042` | Grafana | `WP-098` | `PROPOSED` | Which six operational graphs must exist, and what each is evidence of. | A dashboard displays; it decides nothing. A green dashboard is not an acceptance, and an alert that nobody can act on is not a control. |
| `CMP-046` | buzz-acp — Agent Client Protocol runtime bridge | `WP-048` | `PROPOSED` | The internal `AgentRuntime` contract — `qualify`, `start_session`, `send_task`, `stream_events`, `cancel`, `collect_result`, `close` — and the `AgentRuntimeProfile` that says which runtime may satisfy which requirement. | ACP is a transport and interop boundary, never a scientific domain model. It may not redefine role, evidence, gate or challenge semantics, and an ACP event such as `completed` or `tool succeeded` is an operational observation — acceptance still depends on … |

---

## ADAPTER (6)

| ID | Component | Packages | Status | AETHRION owns | May never decide |
|---|---|---|---|---|---|
| `CMP-004` | LiteLLM | `WP-041` | `PROPOSED` | The `ModelGateway` contract: admission, routing policy, data-class redaction, cost attribution and the fail-closed decision. | The gateway routes and accounts; it never decides whether a model may be used for a task. Admission is WP-044's decision, recorded as a capability profile, and a gateway fallback may not silently substitute a model that was never admitted. |
| `CMP-008` | PaperQA2 | `WP-069` · `WP-070` | `PROPOSED` | How retrieval binds to provenance and claim scope — the retrieval contract, not the retriever. | Retrieval supplies candidates and never a claim. Nothing retrieved becomes evidence until it is anchored as an `EvidenceSpan` against a stored representation. |
| `CMP-009` | OpenScholar | `WP-069` · `WP-071` | `PROPOSED` | The same adapter contract as PaperQA2, deliberately — so the two remain comparable. | Retrieval supplies candidates and never a claim, exactly as for PaperQA2. |
| `CMP-010` | ASReview | `WP-071` | `PROPOSED` | The screening decision record, the stopping rule and the coverage claim that follows from it. | Screening ranks; it never excludes. An exclusion is a recorded human or policy decision with a reason, and a stopping rule reports coverage rather than declaring completeness. |
| `CMP-043` | Zotero | `WP-064` · `WP-067` | `INTEGRATED` | The canonical source registry and the reconciliation rules. An external edit in Zotero does not automatically become truth here. | Zotero is a working library, never the canonical registry. A record's identity, status and licence position are decided by the source registry service; Zotero is one input to it and one projection of it. |
| `CMP-044` | Obsidian | `WP-073` · `WP-074` | `INTEGRATED` | The projection contract, the human/generated zone split and the controlled tag vocabulary. | The vault is a projection and never a source. An edit made by hand inside a generated zone is lost on the next mirror, which is the intended behaviour and not a data-loss defect. |

---

## OPTIONAL_BACKEND (13)

| ID | Component | Packages | Status | AETHRION owns | May never decide |
|---|---|---|---|---|---|
| `CMP-011` | Cedar | `WP-056` | `PROPOSED` | The `PolicyDecision` interface — what WP-056 delivers. The engine behind it is configuration. | The interface is the contract, not the backend. A policy engine answers a question the architecture asked; it may not extend the question, and default deny is a property of the interface rather than of the engine. |
| `CMP-012` | OPA / Rego | `WP-056` | `PROPOSED` | The `PolicyDecision` interface, as for Cedar. | As for Cedar: the interface is the contract, not the backend. |
| `CMP-013` | gVisor | `WP-054` | `PROPOSED` | The `ExecutionBackend` interface and the risk-profile → backend routing. | Isolation technology enforces a boundary the policy decided; it never decides the boundary. A sandbox escape is a control failure, not a permission grant. |
| `CMP-014` | E2B | `WP-054` | `PROPOSED` | The `ExecutionBackend` interface, as for gVisor. | As for gVisor. A hosted backend additionally may not become an egress path that the default-deny proxy has not seen — WP-057. |
| `CMP-015` | S3 Object Lock semantics | `WP-026` | `PROPOSED` | The `ImmutableObjectStore` contract, and the proof that delete and overwrite are actually refused. | The store refuses mutation; it does not decide what deserves to be immutable. Retention class is a policy decision recorded before the object is written. |
| `CMP-016` | lakeFS | `WP-026` | `PROPOSED` | The separation itself — working datasets are not accepted evidence. | A working dataset branch is never an evidence reference. Nothing on a mutable branch may be cited by a claim; promotion into the immutable store is an explicit, recorded act. |
| `CMP-029` | MyST | `WP-090` | `PROPOSED` | The publication package contract, as for Quarto. | As for Quarto. |
| `CMP-045` | Buzz — collaboration relay, teams, rooms and agent identities | `WP-148` · `WP-149` · `WP-150` | `PROPOSED` | The `CollaborationBackend` contract and everything above it: `AgentCohortRecord`, `CognitiveDiversityProfile`, `CommunicationGraph`, `CommunicationEdgePolicy`, `TypedAgentMessage`, `InitialPositionArtifact` and the … | A collaboration backend carries messages and holds no scientific authority whatever. A Buzz message is not a `ClaimVersion`, a Buzz room is not the blackboard, a Buzz workflow is not a gate transition, and a Buzz approval is not a `DecisionRecord`. Removing … |
| `CMP-047` | Hermes — general-purpose cognitive runtime | `WP-048` · `WP-044` | `PROPOSED` | `AgentRuntimeProfile` and its qualification: which runtime may host which cognitive function, with what capabilities, under which clean-context guarantee. | A runtime executes cognition; it defines nothing. Hermes may not decide what a scientific role is, whether a cohort is sufficient, whether a gate passes, whether a claim is accepted, whether a reviewer is independent, whether a protocol may change, or whether … |
| `CMP-048` | Buzz Agent — bundled runtime and `SKILL.md` workspace discovery | `WP-048` · `WP-047` | `PROPOSED` | The Skill Compiler: which skills a given actor may see, materialised as a small task-specific bundle in that actor's worktree. | Discovery decides what a runtime can find; it never decides what an actor may use. A skill reachable through a shared home directory but absent from the compiled bundle is a containment failure, and the canonical skill tree with its vendored provenance stays … |
| `CMP-049` | Codex CLI | `WP-048` | `PROPOSED` | `AgentRuntimeProfile` and its qualification record — which runtime may host which cognitive function, with which capabilities, under which clean-context guarantee. | A runtime executes cognition and defines nothing. It may not decide what a scientific role is, whether a cohort is sufficient, whether a gate passes, whether a claim is accepted, whether a reviewer is independent, or whether a tool effect is authorised. Its … |
| `CMP-050` | Claude Code | `WP-048` | `PROPOSED` | `AgentRuntimeProfile` and its qualification record — which runtime may host which cognitive function, with which capabilities, under which clean-context guarantee. | A runtime executes cognition and defines nothing. It may not decide what a scientific role is, whether a cohort is sufficient, whether a gate passes, whether a claim is accepted, whether a reviewer is independent, or whether a tool effect is authorised. Its … |
| `CMP-051` | OpenCode | `WP-048` | `PROPOSED` | `AgentRuntimeProfile` and its qualification record — which runtime may host which cognitive function, with which capabilities, under which clean-context guarantee. | A runtime executes cognition and defines nothing. It may not decide what a scientific role is, whether a cohort is sufficient, whether a gate passes, whether a claim is accepted, whether a reviewer is independent, or whether a tool effect is authorised. Its … |

---

## STANDARD (5)

| ID | Component | Packages | Status | AETHRION owns | May never decide |
|---|---|---|---|---|---|
| `CMP-022` | Workflow Run RO-Crate | `WP-090` · `WP-082` | `PROPOSED` | Which run facts must be present for a run to be re-executable by someone who was not there. | A crate records what ran. It does not assert that the result supports a claim; that binding is the `EvidenceManifest`'s. |
| `CMP-023` | Croissant 1.1 | `WP-138` | `PROPOSED` | The dataset record and the policy input a usage condition becomes. | A declared usage condition is a policy *input*, never a policy decision. The PDP decides whether a use is permitted. |
| `CMP-024` | SWHID | `WP-014` · `WP-027` | `PROPOSED` | Where a software identity is required in the evidence chain. | An intrinsic identifier establishes identity, not availability and not correctness. Computing one does not archive the code. |
| `CMP-025` | SEPIO | `WP-018` · `WP-075` | `PROPOSED` | The AIRL profile over SEPIO's core — which relation types are used and what each licenses. | A model of assertions is not an assertion about truth. SEPIO gives the shape; the gate decides what may be believed. |
| `CMP-030` | CSL | `WP-090` | `PROPOSED` | Which style a venue requires; nothing more. | A citation style is presentation. It never changes which source supports a claim. |

---

## PATTERN (2)

| ID | Component | Packages | Status | AETHRION owns | May never decide |
|---|---|---|---|---|---|
| `CMP-034` | CaMeL | `WP-136` · `WP-058` | `PROPOSED` | The trusted-control / untrusted-data architecture and the capability gate that enforces it. | Control flow comes from trusted intent. Untrusted content may supply values; it can never create actions or expand permissions. Content crosses the boundary, authority does not. |
| `CMP-035` | Manubot | `WP-090` | `PROPOSED` | Manuscript-as-code discipline: continuous rebuild and citation by identifier. | A build discipline says nothing about what the manuscript may claim. |

---

## What this register is not

It is not an installation list — nothing here is installed, and the two entries marked `INTEGRATED` are the Zotero and Obsidian adapters that already run. It is not a recommendation: an `OPTIONAL_BACKEND` row names a candidate and a way of deciding, never a choice. And it is not evidence of adoption — `status` is the honest field.

## Watched third-party names

A name on this list must resolve to a register entry. The list is a rule set rather than an understanding of what a vendor name looks like, which is why its size is printed on every run: a name nobody added is a name nobody checks.

`ASReview`  `AgentDojo`  `Buzz`  `Cedar`  `Claude Code`  `Codex`  `Croissant`  `GROBID`  `Grafana`  `Hermes`  `Inspect AI`  `Kubernetes`  `Kueue`  `LangGraph`  `Langfuse`  `LinkML`  `LiteLLM`  `MLflow`  `Manubot`  `MyST`  `NATS`  `Neo4j`  `Obsidian`  `OpenCode`  `OpenScholar`  `OpenSearch`  `OpenTelemetry`  `Pandoc`  `PaperQA2`  `PostgreSQL`  `Pub2TEI`  `Quarto`  `SWHID`  `Superpowers`  `Temporal`  `Zotero`  `gVisor`  `lakeFS`  `veraPDF`
