---
title: "Current Status"
cssclasses:
  - aethrion-reference
type: reference
category: evidence
summary: "Every line below is the last line printed by a command that was just run."
source: "docs/STATUS.md"
generated: true
provenance: mirror_vault.py
tags:
  - aethrion/evidence
---

> [!info] Generated view
> This note is generated from `docs/STATUS.md` in the repository. Edit the
> canonical file and regenerate; edits made here are overwritten.

# Current Status

| Field | Value |
|---|---|
| Document type | Generated status page |
| Scope | What the verification bundle reports **right now** |
| Sibling documents | `review/` holds dated, frozen verification reports |
| Status | Regenerated by `scripts/write_status.py` **only when a result changes**; never edited by hand |
| Generated | 2026-08-23T13:54:50+00:00 |

**In one paragraph.** Every line below is the last line printed by a command that was just run. Dated reports under `review/` are evidence and stay frozen; this page is derived and is regenerated, which is how a count stops being something anyone has to remember.

## Checks

| Check | Result |
|---|---|
| Test suite | ✅ 140 passed, 1 warning |
| Skill registry | ✅ all skills conform to the Agent Skills format and the AIRL metadata contract |
| Commissioning plan seal | ✅ 632/632 sealed files verified |
| Skill routing baseline | ✅ the routing baseline holds: every skill is reachable, each still contains its own core rule, and the four confusable pairs remain in different families with both halves routable. That is a precondition for following a procedure, not evidence of having followed one |
| Commissioning plan semantics | ✅ plan semantics OK (0 warning(s)) |
| Programme graph | ✅ the programme is executable: no phase inversion, no cycle across package, scenario and aggregation edges, and every aggregate set equals its registry query |
| Figure semantics | ✅ every mechanically derivable figure claim matches the registries — which is narrower than "the figures are correct", and the number of claims checked is printed so the gap stays visible |
| Document hygiene | ✅ no duplicate heading, broken relative link, unbalanced generated marker or orphaned companion in any governed document |
| Workstream indexes | ✅ 17 directories checked, 0 drift entries |
| Declared counts | ✅ documents agree with the repository and with themselves |
| Stale claims | ✅ no document contradicts the rules above — which is narrower than "nothing is stale", and the count of rules is printed so the gap stays visible |
| Ready queue | ✅ ready queue current — 2 package rows |
| Agent guide | ✅ the agent guide matches the repository |
| Figures | ✅ 14 figures, 0 drift entries |
| Figure containment | ✅ 14 figures checked, 0 overflow(s) |
| Reporting register | ✅ register is auditable: types, sources, retrieval dates, unverified claims and rejections are all present |
| Upstream lineage | ✅ 58 upstream entries checked, 0 with code taken, 1 integrated, 0 lineage problems |
| Obsidian vault | ✅ the vault is well-formed: every link resolves, every generated page names its source, and every tag is in the controlled vocabulary |
| Package analysis blocks | ✅ 160 package documents checked, 0 drift entries |
| Package companions | ✅ 320 companion documents checked, 0 drift entries |

## What these checks do not establish

- **No acceptance.** No work package is `ACCEPTED`; issuance is not acceptance.
- **No behaviour testing.** Skill *routing* has a baseline — every skill is reachable, still carries its own core rule, and stays apart from its confusable pair. Skill *execution* has a fixture corpus and no runtime, and has never run.
- **No end-to-end run.** No research question has travelled G0 → G10.
- **No rendering.** No authoring toolchain is installed, so no document has been rendered.
- **Two checks need a live Bridge** (`mcp_smoke`, `acceptance_v0`) and two need the
  operator's vault (the mirrors); they are not run here.
